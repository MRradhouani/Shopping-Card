let a = array.from(document.getElementById('article'));
let total = document.getElementById('totla');

function total() {
    let sum = 0
    for (let i = 0; i < total.clientHeight; i++)
        total
}

function soum() {
    let a = 0
    for
    a += parseInt()
}